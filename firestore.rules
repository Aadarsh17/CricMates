rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Completely open rules for development to avoid permission errors
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
// Force redeploy timestamp: 2024-05-21T16:45:00Z